# VCP 主服务器地址 (VCPToolBox 的 WebSocket 地址)
VCP_MAIN_SERVER_URL=ws://localhost:3000

# VCP 认证密钥 (与 VCPToolBox config.env 中的 Key 一致)
VCP_KEY=test-key-vcp-agent-fix

# 本节点在 VCP 网络中的名称
SERVER_NAME=SynchAgent-1

# Synch Relay 服务器地址
SYNCH_RELAY_URL=ws://localhost:8080/ws

# 调试模式
DEBUG_MODE=false
